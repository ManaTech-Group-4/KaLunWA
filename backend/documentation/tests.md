## views

- Homepage Events 
  - if only 3 are retrieved
  - if all three are is_featured=true 
  

- Homepage Projects
  - if only 3 are retrieved
  - if all three are is_featured=true 

- Homepage News
  - if only 3 are retrieved
  - if it is the latest

- ImageURL serializer
  - return full resource URL 

