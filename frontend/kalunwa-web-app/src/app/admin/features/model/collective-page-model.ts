export interface CollectivePageModel extends test {
  id: number;
  title: string;
  created_at: string;
  camp: string;
  status: string;
  checked: boolean;
}

export interface test {
  test: string
}
